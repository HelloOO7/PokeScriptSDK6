package core;

public class Random {
	/**
	 * 	Generates a random number withing a range.
	 * 	
	 * 	@param range The maximum value of the random number plus one.
	 */
	static native int Random(int range);
}